version: 2

build:
  os: ubuntu-20.04  # Specify a supported OS
  tools:
    python: "3.9"  # Specify the Python version

sphinx:
  configuration: docs/conf.py  # Path to your Sphinx configuration file

python:
  install:
    - method: setuptools
      path: .