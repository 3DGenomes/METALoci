version: 2

sphinx:
  configuration: docs/conf.py

python:
  version: 3.9
  install:
    - requirements: requirements.txt
    - method: setuptools
      path: .
      extra_requirements:
        - docs
